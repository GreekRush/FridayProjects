# Basic ESPN web scraper.  
This interates through all of the college basketball teams and scrapes their schedules (upcoming games and past results) and writes the results to `output.json`.  


```bash
npm install
npm start
```